# AI Instructor — Phase 2b: نتائج الاختبار قبل الـ Commit

**الفرع:** `ai-instructor-v2` — **التعديلات غير مرفوعة (uncommitted)**
**التاريخ:** 2026-08-02
**`main` لم يُمس:** `e788301572aef295c5e7c98ea5530433e49b4b87`
**لا commit / لا push / لا merge / لا deploy** — بانتظار موافقتك.

---

## 1. التعديلات المطبقة (ملفان فقط)

```
packages/web/src/api/index.ts       | 124 +++++++++++++++++----
packages/web/src/web/pages/chat.tsx |  19 +++---
2 files changed, 118 insertions(+), 25 deletions(-)
```

| # | التعديل | المكان |
|---|---------|--------|
| 1 | **حذف الصفحات المكررة** — `normForDedupe()` + مرور على كامل الترتيب والاحتفاظ بأعلى نسخة فقط | `searchKnowledgeSemantic()` |
| 2 | **حذف الصفحات شبه المكررة** — رفض أي صفحة تشابهها `cosine ≥ 0.95` أو `Jaccard ≥ 0.85` مع صفحة مقبولة سابقاً | `searchKnowledgeSemantic()` |
| 3 | **رفع حد السياق** `9,000` → `24,000` حرف | بناء الـ system prompt |
| 4 | **إصلاح إعادة صياغة السؤال** — `repairRewrite()` يرجّع الاختصارات التي يحذفها `gpt-4o-mini` ويثبّت `TLS` | `translateQueryToEnglish()` |
| 5 | **إعادة تشغيل Reference Slides** — `SEND_REFERENCE_IMAGES = true` + `SHOW_REFERENCE_SLIDES = true` | api + chat.tsx |

**Typecheck:** `npx tsc --noEmit` → **0 أخطاء**
**Production build:** `vite build` → **نجح**

---

## 2. اختبار A/B على الاسترجاع (30 سؤال إنجليزي + عربي)

| المقياس | قبل | بعد |
|---------|-----|-----|
| خانات مكررة (نص متطابق) | **30** | **0** |
| خانات شبه مكررة (أجندة/فهرس) | **41** | **0** |
| صفحات مختلفة فعلياً (من 240 خانة) | **199** | **240** |
| أسئلة اتقصّ سياقها | **16** | **0** |
| حروف ضائعة بالقص | **92,693** | **0** |
| إعادة صياغة تم إصلاحها | — | **11** |

### أوضح الحالات

| السؤال | قبل | بعد |
|--------|-----|-----|
| How does the TLS system work? | **1** صفحة مختلفة من 8 · سياق 3,192 | **8/8** · سياق 9,198 |
| كيف يعمل نظام TLS؟ | **1/8** | **8/8** |
| What are the main components of the TLS? | **2/8** | **8/8** |
| ما هي المكونات الرئيسية لمنظومة TLS؟ | **2/8** | **8/8** |
| What is the TLS agenda of the training course? | **4/8** | **8/8** |
| What are the TLS site survey requirements? | 3/8 · سياق 7,683 | **8/8** · سياق 15,291 |

**السؤال "كيف يعمل نظام TLS؟" قبل التعديل:** كانت الخانات الثمانية كلها صفحات أجندة من نفس الملف
(`TLS_Training_Slides p9, p18, p31, p40, p57, p92, p63`) — لا تحتوي أي إجابة.
**بعده:** `Flight Inspection p7`, `Material 2 p2 (What is TLS?)`, `TLS System Overview p1`,
`Operation p25`, `KSA Overview p34` — صفحات فيها المحتوى الحقيقي.

---

## 3. لماذا 0.95 وليس أقل؟ (دليل الحد الآمن)

فحص pairwise كامل على كل مقاطع الفهرس:

| النطاق | ماذا يحتوي |
|--------|-----------|
| cos ≥ 0.978 | نسخ متطابقة حقيقية / ترويسات مكررة (`020-00074 Rev. G TLS Maintenance Manual`) |
| 0.95 – 0.98 | نسخ وأجندات متكررة (`Technical contents` = 0.9785) |
| **0.90 – 0.95** | **محتوى مختلف فعلاً** — `p32 "Sited on one side of runway"` مقابل `p33 "Sited both sides of runway"` = **0.9487** |

لذلك الحد عند **0.95** — النطاق 0.90–0.95 لا يُمس أبداً حتى لا نفقد محتوى صحيح.

---

## 4. اختبار End-to-End (24 سؤال على السيرفر المحلي)

| المقياس | قبل التعديلات | بعد |
|---------|---------------|-----|
| أسئلة داخل النطاق | 22 | 22 |
| **أجاب** | 19 | **20** |
| رفض (داخل النطاق) | 3 | **2** |
| إجابات فيها مرجع + رقم صفحة | 19 | **20** |
| إجابات فيها صورة صفحة | 19 | **20** |
| أسئلة خارج النطاق | 2 | 2 |
| رُفضت صح | 2 | **2** |
| صور مسرّبة لأسئلة خارج النطاق | 0 | **0** |
| **صور فارغة/بيضاء** | 0 | **0** |
| أخطاء | 0 | **0** |

**"How does the TLS system work?" صار يجيب** — وهو سؤال كان يُرفض على الفرع **وعلى `main` المنشور أيضاً**.

### الرفضان المتبقيان

| السؤال | الحالة على `main` (Railway + Claude Opus) | التفسير |
|--------|------------------------------------------|---------|
| What is the ESA alignment procedure? | **يجيب** | الرفض محلي فقط لأن الاختبار يعمل على `gpt-4o-mini` (الـ fallback) — ليس خطأ في الكود |
| How is the monitor antenna used in TLS? | **يرفض أيضاً** | مشكلة قديمة: المحتوى موجود في `Operation p14` و `Maintenance p97` لكن التشابه الدلالي لا يرفعهما للثمانية الأولى |

### الصور "المفقودة" — إنذار كاذب
سكربت الاختبار أبلغ عن 3 صور غير موجودة على القرص، لأنها ليست ملفات بل نقاط نهاية ديناميكية:

| الرابط | النتيجة الفعلية |
|--------|-----------------|
| `/api/doc-page/72/9` | `200 image/jpeg` · 248,866B · 1300×1683 · sd=43.06 |
| `/api/doc-page/71/7` | `200 image/jpeg` · 365,319B · 1300×1683 · sd=55.24 |
| `/api/doc-page/72/13` | `200 image/jpeg` · 314,200B · 1300×1683 · sd=50.63 |

كلها صور حقيقية غير فارغة.

---

## 5. تعديل جُرّب ثم **تم التراجع عنه**

جرّبت إضافة **hybrid lexical boost** (مطابقة كلمات حرفية فوق التشابه الدلالي) لحل مشكلة
`monitor antenna`. النتيجة:

- **لم يحل المشكلة** — `Operation p14` و `Maintenance p97` ما زالتا خارج الثمانية الأولى.
- **غيّر ترتيب 29 سؤال من 30** وسبّب تراجعاً واضحاً: سؤال `ما معنى كود الخطأ 8010؟`
  فقد صفحة `Maintenance p63` التي كانت الأولى والأصح.

لذلك **تم حذفه بالكامل** — لا أثر له في الكود الحالي.

---

## 6. ملاحظة مهمة عن الموديل في الاختبار المحلي

حساب Anthropic **بلا رصيد**، لذلك الاختبار المحلي يعمل على مسار الـ fallback الحقيقي في الكود
(`gpt-4o-mini`) وهو **أضعف من `claude-opus-4-5`** المستخدم على Railway ويرفض أكثر.
كل رفض محلي تم مقارنته مع Railway قبل تسميته تراجعاً.

كذلك: صفحة الشات ترسل آخر 10 رسائل + سجل المحادثة من قاعدة البيانات مع `userId`،
وهذا السجل القديم كان يدفع `gpt-4o-mini` للرفض. الاختبارات النظيفة (بدون سجل ملوّث)
أجابت 3/3 على نفس السؤال.

---

## 7. Reference Slides — تعمل (لقطات شاشة)

| اللقطة | السؤال | الصورة المرفقة |
|--------|--------|----------------|
| `slides_a1.png` | How does the TLS system work? | `Material 2 (Presentation) p1` · 1300×975 |
| `slides_a2.png` | ما هي ترددات الاستجواب والرد في منظومة TLS؟ | `GetAtt Document p6` · 1300×1683 |
| `slides1.png` | What is the TLS interrogation frequency? | `Analysis Tools p33` · 1300×1683 |

القسم يظهر بعنوان **REFERENCE SLIDES** أسفل الإجابة، مع زر تكبير، والصور حقيقية من الصفحة المذكورة.

**مهم:** `/doc-pages/*` تُخدم من سيرفر Bun فقط (`packages/web/src/server.ts`) وليس من `vite dev`،
لذلك اللقطات أُخذت من **build إنتاجي** يعمل على المنفذ 4400.

---

## 8. الحالة الحالية

| العنصر | الحالة |
|--------|--------|
| commit محلي على `ai-instructor-v2` | **`a55513d`** — تم ✅ |
| `ai-instructor-v2` على GitHub | `aeff454a…` — **لم يُدفع** ⛔ (بأمرك) |
| `main` على GitHub | `e788301…` — **لم يُمس** ✅ |
| merge إلى `main` | لم يحدث ⛔ |
| Railway (منشور) | يعمل على `main` كما هو — **لم يُنشر شيء** ⛔ |
| ملف توكن GitHub | حُذف من القرص ✅ |
| `tsc --noEmit` | 0 أخطاء ✅ |

التعديلات محفوظة في commit محلي واحد (`a55513d`) فوق `aeff454`. لا شيء خرج من السandbox.

**بانتظار مراجعتك.** لا push ولا merge ولا deploy إلا بأمر صريح منك.
